"""Blog app — placeholder structure (per plan 003).

Real models, listing pages, and tags land in a later module. The initial
scaffold ships only a top-level :class:`BlogIndexPage` so the homepage
can link to ``/blog/`` from day one.
"""
