{
  "name": "@asalichoice/storefront-theme",
  "version": "0.1.0",
  "private": true,
  "description": "Tailwind CSS build pipeline for the AsliChoice storefront theme. Standalone (no django-tailwind) — see plans/phase-0-foundation/003-website-wagtail-setup.md step 2.",
  "scripts": {
    "tailwind:build": "tailwindcss -i ./input.css -o ../static/theme/css/output.css --minify",
    "tailwind:watch": "tailwindcss -i ./input.css -o ../static/theme/css/output.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17",
    "@tailwindcss/typography": "^0.5.15",
    "@tailwindcss/forms": "^0.5.9"
  }
}
