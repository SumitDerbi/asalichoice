# Vite (admin-ui)
# Copied to admin-ui/.env for local development. Real values live under
# ~/deploy_config/<env>/<env>.env on the deploy host.
VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_APP_NAME=AsliChoice Admin
VITE_APP_VERSION=0.1.0
