node_modules
# admin-ui/dist is committed as a deploy artifact (low-mem shared host).
# Build locally with `npm run build --workspace admin-ui` before pushing.
# dist
dist-ssr
coverage
playwright-report
test-results
.env
.env.*
!.env.example
!.env.production
*.local
.vite
.DS_Store
.vscode/*
!.vscode/extensions.json
