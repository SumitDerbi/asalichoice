# Production / staging env baked into the admin-ui Vite build.
# Used when running: npm run build --workspace admin-ui
# Vite picks this up automatically because mode === 'production'.
VITE_API_BASE_URL=https://app.aslichoice.com/api/v1
VITE_APP_NAME=AsliChoice Admin
VITE_APP_VERSION=0.1.0
