"""``apps.system_settings`` — earliest slice of Module 18.

Provides site settings, feature toggles, integration keys (encrypted),
social links, and contact info so later modules can read configuration
from the DB rather than hardcoding values.
"""
