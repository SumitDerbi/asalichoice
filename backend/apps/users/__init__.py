"""Custom user model + JWT auth surface (phase-0 skeleton).

The full identity / roles / OTP story lands in M02; this app provides
the minimum the rest of phase 0 needs: a swappable ``AUTH_USER_MODEL``,
a JWT login endpoint, refresh + logout, and ``/me/``.
"""
