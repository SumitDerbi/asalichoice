"""
Settings package for the AsliChoice backend.

Import the environment-specific module via the ``DJANGO_SETTINGS_MODULE``
environment variable. The defaults are::

    config.settings.development   # local dev (manage.py)
    config.settings.production    # wsgi/asgi (cPanel / gunicorn)

See ``base.py`` for shared configuration.
"""
